name: Check Licenses

on: [pull_request]

jobs:
    check:
        runs-on: ubuntu-latest

        steps:
            -   name: Checkout code
                uses: actions/checkout@v2

            -   name: Check licenses
                uses: EmbarkStudios/cargo-deny-action@v2
